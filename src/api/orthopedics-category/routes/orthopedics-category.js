'use strict';

/**
 * orthopedics-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::orthopedics-category.orthopedics-category');
